

//Creates a div and appends it to the body.
// Adding an event listener to check if the DOM is loaded so that we don't access anything before it is unloaded.
    // console.log("Anything happen??");
    // const div = document.createElement("div");
    // div.id = "overlay-div";
    // div.innerHTML = "Overlaying baby";
    // let body = document.querySelector("body");
    // console.log(div);
    // body.appendChild(div);
  